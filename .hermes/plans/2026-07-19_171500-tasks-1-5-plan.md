# Tasks 1–5 Implementation Plan

> **Goal:** Fix Node 20 warnings → Security issues → Build times → Build warnings → Auth Token button
> 
> **Orchestration:** Each task gets its own branch + PR, delegate work where parallelizable

---

## Prerequisite Check

- [x] Main branch up-to-date (`94391df`)
- [x] `perf/issue-20-ci-build-optimization` already merged into main 
- [x] Code scanning alerts visible (5 alerts, 2 open errors, 2 open warnings)
- [x] Only issue #25 open on GitHub

---

## Task 1: Fix Node.js 20 Warnings (→ Upgrade Actions)

**Issue:** `actions/upload-artifact@v4`, `gradle/actions/setup-gradle@v4`, `actions/download-artifact@v4` target Node 20; GitHub runner defaults to Node 24.

**Approach:** Check and upgrade to v5 of these actions where available. 
- `upload-artifact` v5 exists (as `@v5`)
- `download-artifact` v5 exists (as `@v5`)
- `gradle/actions/setup-gradle` — check if v5 exists

**Files:**
- `.github/workflows/build_android.yaml`

---

## Task 2: Fix Security Issues (Issue #25)

**Code scanning alerts to fix:**
1. **Alert 5** (warning) — `skills/built-in/qr-code/scripts/index.html` — Untrusted source inclusion
2. **Alert 4** (warning) — `Android/src/app/src/main/assets/skills/qr-code/scripts/index.html` — Untrusted source inclusion  
3. **Alert 3** (error) — `skills/featured/mood-music/assets/webview.html` — Client-side URL redirect
4. **Alert 2** (error) — `skills/featured/mood-music/assets/webview.html` — Client-side XSS

**Files:**
- Various HTML files under `skills/` and `Android/src/`

---

## Task 3: Improve Build Times

**Current state:** Already has `-Dorg.gradle.parallel=true`, Gradle build cache, AGP 8.13, Gradle 8.13. Additional ideas:
- Enable Gradle Build Cache for CI (already enabled)
- Use `--configuration-cache` (Gradle 8.13 supports it)
- Check if `org.gradle.caching=true` in gradle.properties
- Enable parallel test execution in CI
- Reduce artifact upload paths

---

## Task 4: Fix Open Warnings in Build Logs

**Known warnings to hunt:**
- Moshi Kapt → KSP migration warning
- Any lint warnings from CI output
- Deprecation warnings

---

## Task 5: Add Auth-Token-Button

**Files:**
- Server tab UI in Kotlin/Compose code
- Random token generation function

---
